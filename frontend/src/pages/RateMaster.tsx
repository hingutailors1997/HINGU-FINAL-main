import React from 'react';
import RateMasterTab from '../components/employees/RateMasterTab';

export default function RateMaster() {
  return (
    <div className="animate-in fade-in duration-500 relative">
      <RateMasterTab />
    </div>
  );
}
